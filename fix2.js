const fs = require('fs'); let c = fs.readFileSync('actions/client-history-actions.ts', 'utf8'); c = c.replace('export async function getClientHistory(clientId: string): Promise<{\r\n    await requireAuth();', 'export async function getClientHistory(clientId: string): Promise<{'); c = c.replace('    error?: string\r\n}> {\r\n    const insforge', '    error?: string\r\n}> {\r\n    await requireAuth();\r\n    const insforge'); fs.writeFileSync('actions/client-history-actions.ts', c);