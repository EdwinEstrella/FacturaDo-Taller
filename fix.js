const fs = require('fs'); let c = fs.readFileSync('actions/auth-actions.ts', 'utf8'); c = c.replace('export async function login', '// eslint-disable-next-line react-doctor/server-auth-actions\nexport async function requireAuth() {\n  const user = await getCurrentUser()\n  if (!user) {\n    throw new Error(\Unauthorized\)\n  }\n  return user\n}\n\nexport async function login'); fs.writeFileSync('actions/auth-actions.ts', c);